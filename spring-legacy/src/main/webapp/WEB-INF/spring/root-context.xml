<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">

	<!-- Root Context: defines shared resources visible to all other web components -->
	<!-- Root Context - 서버 구동과 동시에 web.xml파일을 거쳐 로딩되는 문서 - 서버 구동과 동시에 세팅하여야 
		하는 내용들을 작성하며, 작성한 내용은 어플리케이션 전역에서 사용된다. -->
	<context:property-placeholder
		location="classpath:driver.properties" />

	<!-- DB 연결설정 -->
	<!-- 1) DB 연결도구 설정 -->
	<bean class="org.apache.commons.dbcp.BasicDataSource"
		id="datasource" destroy-method="close">
		<property name="driverClassName"
			value="${datasource.driverClassName}" />
		<property name="url" value="${datasource.url}" />
		<property name="username" value="${datasource.username}" />
		<property name="password" value="${datasource.password}" />
	</bean>
	<!-- 2) MyBatis spring 제공 sql 명령실행도구 등록 -->
	<bean class="org.mybatis.spring.SqlSessionFactoryBean"
		id="sqlSessionFactory">
		<!-- mybatis 설정파일 위치 지정 -->
		<property name="configLocation"
			value="classpath:mybatis-config.xml" />
		<!-- DB 연결도구 -->
		<property name="dataSource" ref="datasource" />
		<!-- mapper 파일 위치등록 -->
		<property name="mapperLocations"
			value="classpath*:mapper/**/*.xml"></property>
	</bean>
	<!-- 3) sql구문을 실행하기 위한 템플릿을 제공하는 bean객체 등록 -->
	<bean class="org.mybatis.spring.SqlSessionTemplate"
		id="sqlSession">
		<constructor-arg ref="sqlSessionFactory" />
	</bean>

	<!-- transactionManager - 트랜젝션 처리하는 bean객체 - JDBC를 통해 커넥션을 얻어온 후, 트랜젝션 시작/커밋/롤백 
		담당 - @Transactional 어노테이션을 통해 트랜젝션을 선언적으로 처리 -->
	<bean id="transactionManager"
		class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="datasource" />
	</bean>

	<!--
		multipartResolver
			- http 요청 중 content type이 multipart(파일업로드)일 때 담당하여 처리
			- request 객체를 MultipartRequest 객체로 자동 변경
	-->
	<bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<!--
			maxUploadSize : 업로드되는 전체 파일의 크기 제한(바이트 단위)
			maxInMemorySize : 업로드 처리시 디스크에 파일을 생성하기 전 메모리에 보관할 파일의 최대 크기 설정. 업로드 파일의 크기가 더 크면 해당 파일을 디스크에 임시 보관
		-->
		<property name="defaultEncoding" value="utf-8"/>
		<property name="maxUploadSize" value="10000000"/>
		<property name="maxInMemorySize" value="2000000"/>		
	</bean>
</beans>
